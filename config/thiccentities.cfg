# Configuration file

artifacts {

    mimic {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


beneath {

    shadow {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


betterswim {

    herobrine {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


coroutil {

    bat_smart {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


da {

    mini_blossom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    nether_aberant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spore_cloud {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    void_spike {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    boss_spawner {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    nether_pyre {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    flame_knight {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ancient_knight {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ancient_knight_rapier {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ancient_fallen {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rot_spike {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    night_lich {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lich_staff {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lich_spawn {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wyrk {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ice_spike {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frost_draugr {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frost_draugr_ranger {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frost_draugr_elite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wyrk_foot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


dimdoors {

    mob_monolith {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


enderskills {

    animated_stone_golem {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    voltaic_sentinel {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


erebus {

    erebus {

        beetle_larva {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        wasp {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        centipede {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        beetle {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        fly {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        mosquito {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        tarantula {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        bot_fly {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        scorpion {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        solifuge {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        grasshopper {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        locust {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        moth {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        rhino_beetle {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        antlion {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        black_widow {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        glow_worm {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        bombardier_beetle {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        scytodes {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        money_spider {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        praying_mantis {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        jumping_spider {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        fire_ant {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        worker_bee {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        velvet_worm {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        dragon_fly {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        titan_beetle {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        bot_fly_larva {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        fungal_weevil {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        crop_weevil {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        woodlouse {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        cicada {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        fire_ant_soldier {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        lava_web_spider {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        antlion_mini_boss {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        chameleon_tick {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        solifuge_small {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        midge_swarm {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        punchroom {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        crushroom {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        black_ant {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        zombie_ant {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        tarantula_mini_boss {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        tarantula_baby {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        pond_skater {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        bog_maw {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        magma_crawler {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        animated_block {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        animated_chest {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        animated_bamboo_crate {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        umber_golem {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        umber_golem_idol {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        antlion_boss {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        honey_pot_ant {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        bombardier_beetle_larva {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        zombie_ant_soldier {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        bed_bug {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        stag_beetle {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

        gas_vent {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

    }

}


eyesinthedarkness {

    eyes {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


grue {

    entitygrue {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


minecraft {

    elder_guardian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wither_skeleton {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    stray {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    husk {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie_villager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton_horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie_horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    donkey {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mule {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    evocation_illager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vex {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vindication_illager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    illusion_illager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    giant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ghast {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie_pigman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    enderman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cave_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    silverfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=0.5

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    blaze {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=0.6

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=0.1

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    magma_cube {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wither {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    witch {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    endermite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    guardian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    shulker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pig {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sheep {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    chicken {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    squid {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wolf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mooshroom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    snowman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ocelot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    villager_golem {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rabbit {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    polar_bear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    llama {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    parrot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    villager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    player {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity's reach distance by. Default: 1
        D:reach=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


multimob {

    dummy {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


primitivemobs {

    chameleon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    treasure_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    haunted_tool {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    grovesprite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bewitched_tome {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    filch_lizard {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    brain_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rocket_creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    festive_creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    support_creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton_warrior {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    blazing_juggernaut {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lily_lurker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mother_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    baby_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    trollager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lost_miner {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    traveling_merchant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dodo {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mimic {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sheepman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    goblin {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    harpy {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    flame_spewer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.5

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    void_eye {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


quark {

    stoneling {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    foxhound {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dweller {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ashen {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frog {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pirate {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    crab {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wraith {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    leash_knot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


roughmobsrevamped {

    bosszombie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bossskeleton {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hostilebat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


spiderstpo {

    better_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    better_cave_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


srparasites {

    sim_bigspider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_human {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_cow {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_sheep {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_wolf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_pig {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_villager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_adventurer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_bear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_enderman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_sheephead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_wolfhead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_cowhead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_pighead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_villagerhead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_horsehead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sim_humanhead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    incompleteform_small {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    incompleteform_medium {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    host {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    carrier_heavy {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buglin {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    carrier_flying {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rupter {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    movingflesh {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    worker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    beckon_si {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    beckon_sii {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    beckon_siii {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    beckon_siv {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kyphosis {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sentry {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pri_longarms {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pri_manducater {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pri_reeker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pri_yelloweye {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pri_summoner {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pri_bolster {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pri_arachnida {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ada_longarms {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ada_manducater {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ada_reeker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ada_yelloweye {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ada_summoner {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ada_bolster {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ada_arachnida {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    overseer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vigilante {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    warden {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bomber_light {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    marauder {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    grunt {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    anc_dreadnaut {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    anc_overlord {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    anc_pod {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    anc_dreadnaut_ten {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    homming {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    biomass {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tendril {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


srrevenants {

    for_cow {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    for_bear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    for_horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    for_human {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    for_pig {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    for_sheep {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


weeping-angels {

    anomaly {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    weepingangel {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


